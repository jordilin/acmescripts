#!/bin/sh

set -o pipefail

echo "Testing ...\n"
cargo test -q | sed -n '/^failures/,/failures/{s/^failures //;/^failures/d;p;}'

if [ $? -eq 0 ]
then
	echo "All good ;-)\n"
else
	echo "Oops. FAILED!!\n"
fi
